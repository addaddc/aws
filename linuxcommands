1.Refresh repo by typing the following command:
yum repolist
2.
sudo yum list

3. show amazon linux kernel
rpm -E %{rhel}
For Amazon Linux 2, this will give you 7.


cat /etc/os-release
cat /proc/version

4.
sudo dnf install https://dl.fedoraproject.org/pub/epel/epel-release-latest-8.noarch.rpm
